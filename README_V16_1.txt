Stormkeeper V16.1

Mobile-friendly patch. Upload these root files over the existing repository files. Keep all existing JSON files. index.html contains the CSS inline so styling does not depend on a css/ folder. app.js is expected at repository root.
